/**
 * PrometheusLiveCostPanel — thin wrapper kept for backward-compatibility.
 *
 * All logic, state, and rendering now lives in ClusterCostDashboard and the
 * Cc*Page components in the same directory.
 */
import ClusterCostDashboard from "./ClusterCostDashboard";
export default ClusterCostDashboard;
